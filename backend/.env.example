APP_NAME=Project Name

DATABASE_URL=postgresql://name:password@localhost:5432/db

SECRET_KEY=supersecretkey
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional: SMTP / Email settings (used for OTPs, cancellations, notifications)
# Example using SMTPS (SSL) — common with many providers (port 465)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_SSL=1
EMAIL_FROM=pirzadahasnain@gmail.com

# Example using STARTTLS (port 587)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-smtp-password-or-app-password
# SMTP_USE_SSL=0
# EMAIL_FROM=your-email@example.com

# Notes:
# - For Gmail use an App Password (requires 2FA). Use SMTP_HOST=smtp.gmail.com.
# - After adding these variables, restart the backend server (uvicorn) so the new settings are picked up.
